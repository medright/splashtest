<h1>Patient:</h1>

<ul>
<% @patient.each do |patient|%>
	<h2><%= link_to(patient.first_name, patient_path(patient)) %></h2>
	<li><%= patient.first_name %>, <%= patient.last_name %> <%= patient.dob %> <%= patient.allergies %></li>
<% end %>
</ul>
</div>
<%= link_to("Add Patient", new_patient_path) %>
